export default function CtaStrip() {
  return (
    <section className="cta-strip" id="how">
      <div className="container cta-inner">
        <div>
          <p className="eyebrow cta-eyebrow">3 Simple Steps</p>
          <h2>Schedule a pickup in minutes</h2>
          <p>Book online, drop it off or have us collect it — then follow every step in the app.</p>
        </div>
        <a className="btn btn-secondary" href="#pickup">
          Schedule Pickup
        </a>
      </div>
    </section>
  );
}
